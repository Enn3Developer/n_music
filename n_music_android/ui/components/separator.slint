import {Palette} from "std-widgets.slint";

export component Separator inherits HorizontalLayout {
    alignment: center;
    Rectangle {
        width: parent.width - 20px;
        height: 1px;
        background: Palette.border;
    }
}
